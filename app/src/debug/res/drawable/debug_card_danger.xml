<?xml version="1.0" encoding="utf-8"?>
<!-- Same card shell as debug_card_background, tinted red - marks the RESET
     section as the one destructive action on this screen, distinct from the
     violet "test tool" cards around it. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/surface" />
    <corners android:radius="18dp" />
    <stroke android:width="1dp" android:color="@color/low" />
</shape>
