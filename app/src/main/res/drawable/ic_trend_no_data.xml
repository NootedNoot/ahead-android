<?xml version="1.0" encoding="utf-8"?>
<!-- Deliberately not an arrow shape - this is the small icon shown when the
     last reading is missing or stale, so it must read as visually distinct
     from every trend arrow at a glance in the status bar. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:fillColor="#FFFFFFFF"
        android:pathData="M4,10 L20,10 L20,14 L4,14 Z" />
</vector>
